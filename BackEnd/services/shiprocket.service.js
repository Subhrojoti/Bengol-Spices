// services/shiprocket.service.js

export const createShiprocketOrder = async (order) => {
  // ⚠️ This will be implemented later
  // For now, just return dummy response

  return {
    provider: "SHIPROCKET",
    awb: null,
    trackingUrl: null,
    status: "CREATED",
  };
};
